# Used for Compose interpolation, NOT the host application's .env.
APP_ENV_FILE=.env.docker
API_REPLICAS=2
# Optional image names, useful when moving builds to CI.
API_IMAGE=shortlink-api:local
MIGRATION_IMAGE=shortlink-migration:local
REDIS_CLUSTER_IMAGE=shortlink-redis-cluster:local
