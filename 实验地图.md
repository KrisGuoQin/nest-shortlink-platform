# 带 Workspace / RBAC 权限体系的短链接分享服务

最终把它从一个普通 CRUD 项目，演进成一个具备 10 万级用户架构思维 的后端系统。

## 01 Node.js HTTP Server 并发基础

自己写 HTTP Server；理解请求生命周期、同步阻塞、异步 IO、并发 ≠ 多线程

## 02 Event Loop / Blocking 实验

Timer、Promise、I/O、CPU 密集任务；观察 Event Loop 被阻塞之后 QPS 如何下降

## 03 Worker Threads / CPU 密集任务

把 CPU 密集计算移出主线程；理解 Worker Thread 适合解决什么问题

## 04 NestJS 工程化基础

Module、Controller、Service、DTO、Validation、异常处理；搭建项目骨架

## 05 RBAC 权限系统

User、Role、Permission；Guard、Decorator；实现角色与权限控制

## 06 Workspace 多租户权限

Tenant / Workspace 数据隔离；成员、角色、资源归属；避免越权访问

## 07 JWT 身份认证

Access Token、Refresh Token、Guard、登录状态、Token 刷新

## 08 PostgreSQL + Prisma 数据层

Schema、Migration、Relation、Repository 思维、数据库连接

## 09 数据库索引与慢查询

B-Tree、联合索引、最左前缀、EXPLAIN、避免全表扫描

## 10 短链接核心业务

Snowflake/NanoID/Hash、短码生成、重定向、冲突处理

## 11 Redis Cache Aside

Redis 缓存、TTL、Cache Aside、缓存更新/失效策略

## 12 Redis 分布式锁

**SET NX PX**、锁超时、UUID Token、Lua compare-and-delete

## 13 Nginx + NestJS 多实例

Reverse Proxy、负载均衡、多个 Nest 实例、无状态服务

## 14 Docker / Docker Compose

NestJS、PostgreSQL、Redis 容器化；搭建完整本地运行环境

## 15 Prometheus Metrics

请求数、QPS、延迟、错误率、Node.js Runtime 指标

## 16 Grafana Dashboard

Prometheus + Grafana；QPS、P95/P99、CPU、Memory、Event Loop Lag

## 17 高并发压测

autocannon / k6；并发数、吞吐量、延迟、P95/P99；寻找系统瓶颈

## 18 Redis 性能与缓存体系

Redis Pipeline、批量操作、连接复用、缓存命中率等优化

## 19 Redis Cluster / Stampede / Hot Key / Bloom Filter

Redis Cluster、Hash Slot、缓存击穿、雪崩、穿透、热点 Key、L1 Cache、Bloom Filter

## 20 Redis Cluster 故障转移

kill master、Replica 提升、Failover、MOVED、ioredis 自动恢复

## 21 RabbitMQ / Kafka 消息队列

异步化、削峰填谷、Producer/Consumer、ACK、重试、DLQ、消息幂等

## 22 BullMQ 任务系统

Worker、Queue、并发任务、延迟任务、定时任务、Retry、后台 Job

## 23 数据库高并发

事务、隔离级别、乐观锁、悲观锁、连接池、批量写、并发更新

## 24 限流 / 熔断 / 降级

oken Bucket、Redis 分布式限流、Circuit Breaker、Fallback、服务保护

## 25 最终综合架构压测

Nginx + 多实例 NestJS + Redis Cluster + MQ + PostgreSQL + Prometheus/Grafana；完整压测与瓶颈分析
